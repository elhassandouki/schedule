@extends('adminlte::page')
@section('title', 'Professeurs')
@section('content_header')<div class="d-flex justify-content-between"><h1>Gestion des professeurs</h1><a href="{{ route('professors.create') }}" class="btn btn-primary">Ajouter un professeur</a></div>@endsection
@section('content')
@if(session('success'))<div class="alert alert-success">{{ session('success') }}</div>@endif
<div class="card"><div class="card-body table-responsive p-0"><table class="table table-hover"><thead><tr><th>Professeur</th><th>Limite / semaine</th><th>Modules autorisés</th><th>Disponibilités</th><th></th></tr></thead><tbody>@forelse($professors as $professor)@php $avails = \Illuminate\Support\Facades\DB::table('professor_availabilities')->where('professor_id', $professor->id)->orderBy('day_of_week')->get(); @endphp<tr><td>{{ $professor->name }}<br><small>{{ $professor->email }}</small></td><td>{{ $professor->max_weekly_hours ?? 'Sans limite' }}</td><td>{{ $professor->modules->pluck('code')->join(', ') ?: 'Aucun module' }}</td><td>@if($avails->isEmpty())<span class="badge badge-warning">Non définies (tous les jours)</span>@else @foreach($avails as $a)<span class="badge badge-{{ $a->available?'success':'secondary' }}" title="{{ ['Lu','Ma','Me','Je','Ve','Sa','Di'][$a->day_of_week-1] ?? 'Jour '.$a->day_of_week }}">{{ ['Lu','Ma','Me','Je','Ve','Sa','Di'][$a->day_of_week-1] ?? '' }} {{ intdiv($a->start_minute,60) }}h{{ str_pad($a->start_minute%60,2,'0',STR_PAD_LEFT) }}-{{ intdiv($a->end_minute,60) }}h{{ str_pad($a->end_minute%60,2,'0',STR_PAD_LEFT) }}</span> @endforeach @endif</td><td><a href="{{ route('professors.availabilities', $professor) }}" class="btn btn-sm btn-outline-success">Disponibilités</a> <a href="{{ route('professors.edit', $professor) }}" class="btn btn-sm btn-outline-primary">Modifier</a></td></tr>@empty<tr><td colspan="5" class="text-center">Aucun professeur.</td></tr>@endforelse</tbody></table></div>@if($professors->hasPages())<div class="card-footer">{{ $professors->links() }}</div>@endif</div>
@endsection
