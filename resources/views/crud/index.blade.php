@extends('adminlte::page')
@section('title', $meta['title'])
@section('content_header')<div class="d-flex justify-content-between align-items-center"><h1>{{ $meta['title'] }}</h1><a href="{{ route('crud.create',$resource) }}" class="btn btn-primary"><i class="fas fa-plus"></i> Ajouter</a></div>@endsection
@section('content')
@if(session('success'))<div class="alert alert-success">{{ session('success') }}</div>@endif @if(session('error'))<div class="alert alert-danger">{{ session('error') }}</div>@endif
<div class="card"><div class="card-body table-responsive p-0"><table class="table table-hover"><thead><tr>@foreach($meta['fields'] as $field=>$label)<th>{{ $label }}</th>@endforeach<th class="text-right">Actions</th></tr></thead><tbody>@forelse($rows as $row)<tr>@foreach($meta['fields'] as $field=>$label)<td>@if(($meta['types'][$field]??'')==='checkbox')<span class="badge badge-{{ $row->$field?'success':'secondary' }}">{{ $row->$field?'Oui':'Non' }}</span>@elseif(isset($choices[$field])){{ $choices[$field][$row->$field] ?? '—' }}@elseif($field==='password')••••••••@else{{ $row->$field }}@endif</td>@endforeach<td class="text-right"><a href="{{ route('crud.edit',[$resource,$row->id]) }}" class="btn btn-sm btn-outline-primary"><i class="fas fa-edit"></i></a><form class="d-inline" method="post" action="{{ route('crud.destroy',[$resource,$row->id]) }}" onsubmit="return confirm('Supprimer cet enregistrement ?')">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger"><i class="fas fa-trash"></i></button></form></td></tr>@empty<tr><td colspan="{{ count($meta['fields'])+1 }}" class="text-center text-muted py-4">Aucune donnée. Cliquez sur « Ajouter ».</td></tr>@endforelse</tbody></table></div>@if($rows->hasPages())<div class="card-footer">{{ $rows->links() }}</div>@endif</div>
@endsection
